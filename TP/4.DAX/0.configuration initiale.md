# Guide de Configuration Détaillé - SSAS Tabular

## Configuration complète du projet AdventureWorks

---

## PARTIE 1 : Après création du projet Tabular

### Étape 1 : Créer le projet (vous êtes ici ✓)

1. Visual Studio → **Fichier** → **Nouveau** → **Projet**
2. Rechercher : **Analysis Services Tabular Project**
3. Nom du projet : `AdventureWorksTabular`
4. Niveau de compatibilité : **1500** ou supérieur
5. **Workspace Server** : `localhost` (votre serveur SSAS local)
6. Cliquer **OK**

**Résultat** : Vous voyez maintenant l'Explorateur de modèles tabulaires (comme votre screenshot)

---

## PARTIE 2 : Importer les données (ÉTAPES DÉTAILLÉES)

### Étape 2.1 : Lancer l'assistant d'importation

**Dans l'Explorateur de modèles** (votre fenêtre actuelle) :

1. Clic droit sur **"Sources de données"** (Data Sources)
2. Sélectionner **"Nouvelle source de données..."**

**OU** dans le menu :

- **Modèle** → **Importer à partir d'une source de données**

### Étape 2.2 : Choisir la source de données

Une fenêtre **"Obtenir des données"** s'ouvre :

1. Sélectionner **"SQL Server"** ou **"Base de données SQL Server"**
2. Cliquer **"Connexion"** ou **"Suivant"**

### Étape 2.3 : Configurer la connexion SQL Server

Dans la fenêtre de connexion :

```
┌─────────────────────────────────────────────┐
│ Se connecter à SQL Server                   │
├─────────────────────────────────────────────┤
│ Nom du serveur : localhost                  │
│                  (ou .\SQLEXPRESS)          │
│                  (ou nom_serveur)           │
│                                             │
│ Authentification :                          │
│ ○ Authentification Windows  ← RECOMMANDÉ   │
│ ○ Authentification SQL Server              │
│                                             │
│ Base de données :                           │
│ [v] Sélectionner une base de données        │
│ Dropdown : AdventureWorksDW  ← IMPORTANT   │
│                                             │
│ [ Tester la connexion ]                     │
│                                             │
│        [Annuler]  [OK]                      │
└─────────────────────────────────────────────┘
```

**Paramètres à saisir** :

- **Nom du serveur** : `localhost` ou `.\SQLEXPRESS` ou votre nom de serveur
- **Authentification** : Choisir **Authentification Windows** (plus simple)
- **Base de données** : Sélectionner **AdventureWorksDW** dans la liste déroulante
3. Cliquer **"Tester la connexion"** pour vérifier
4. Si OK → Cliquer **"OK"**

### Étape 2.4 : Sélectionner les tables

Une fenêtre **"Navigateur"** s'affiche avec la liste des tables :

```
┌─────────────────────────────────────────────────────────────┐
│ Sélectionner les tables et les vues                         │
├─────────────────────────────────────────────────────────────┤
│ Rechercher : [________________]                              │
│                                                              │
│ ☐ Sélectionner tout                                          │
│ ├─ dbo                                                       │
│ │  ☐ DatabaseLog                                            │
│ │  ☑ DimCustomer                      ← COCHER              │
│ │  ☑ DimDate                          ← COCHER              │
│ │  ☐ DimEmployee                                            │
│ │  ☑ DimGeography                     ← COCHER              │
│ │  ☑ DimProduct                       ← COCHER              │
│ │  ☑ DimProductCategory               ← COCHER              │
│ │  ☑ DimProductSubcategory            ← COCHER              │
│ │  ☐ DimPromotion                                           │
│ │  ☑ DimSalesTerritory                ← COCHER              │
│ │  ☑ FactInternetSales                ← COCHER              │
│ │  ☑ FactResellerSales                ← COCHER              │
│ │  ☐ ...autres tables...                                    │
│                                                              │
│        [Annuler]  [Charger]  [Transformer les données]      │
└─────────────────────────────────────────────────────────────┘
```

**COCHER les tables suivantes** (et SEULEMENT celles-ci pour commencer) :

**Tables de dimensions** :

- ☑ **DimCustomer**
- ☑ **DimDate**
- ☑ **DimGeography**
- ☑ **DimProduct**
- ☑ **DimProductCategory**
- ☑ **DimProductSubcategory**
- ☑ **DimSalesTerritory**

**Tables de faits** :

- ☑ **FactInternetSales**
- ☑ **FactResellerSales**

**Total : 9 tables à cocher**

### Étape 2.5 : Charger les données

1. **Cliquer sur "Charger"** (pas "Transformer les données")

**Résultat** :

- Une barre de progression apparaît
- Les tables sont importées dans le modèle
- Cela peut prendre 1-3 minutes

```
Chargement en cours...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 45%
Importation de FactInternetSales... 60,398 lignes
```

---

## PARTIE 3 : Vérifier l'importation

### Étape 3.1 : Vérifier dans l'Explorateur de modèles

Dans **l'Explorateur de modèles tabulaires**, vous devriez maintenant voir :

```
Explorateur de modèles tabulaires
├─ 📊 Modèles
│  ├─ 📁 TechSmartTabular
│  │  ├─ 📁 Expressions
│  │  ├─ 📁 Groupes de calcul
│  │  ├─ 📁 Mesures
│  │  ├─ 📁 Perspectives
│  │  ├─ 📁 Relations                    ← IMPORTANT
│  │  ├─ 📁 Rôles
│  │  ├─ 📁 Sources de données
│  │  │  └─ 📄 SqlServer localhost AdventureWorksDW  ← Votre source
│  │  ├─ 📁 Tables                       ← IMPORTANT
│  │  │  ├─ 📄 DimCustomer
│  │  │  ├─ 📄 DimDate
│  │  │  ├─ 📄 DimGeography
│  │  │  ├─ 📄 DimProduct
│  │  │  ├─ 📄 DimProductCategory
│  │  │  ├─ 📄 DimProductSubcategory
│  │  │  ├─ 📄 DimSalesTerritory
│  │  │  ├─ 📄 FactInternetSales
│  │  │  └─ 📄 FactResellerSales
│  │  └─ 📁 Traductions
```

**✅ Si vous voyez les 9 tables** : Importation réussie !

### Étape 3.2 : Voir les données en mode Grille

1. Dans **l'Explorateur de modèles**, développer **"Tables"**
2. **Double-cliquer** sur **FactInternetSales**

**Résultat** : Vous voyez les données dans une vue tableur :

```
SalesOrderNumber | OrderDate  | CustomerKey | ProductKey | SalesAmount | ...
SO43659         | 2011-07-01 | 11000       | 310        | 3399.99     | ...
SO43660         | 2011-07-01 | 11001       | 311        | 2443.35     | ...
...
```

**Vérifiez** :

- Les colonnes sont présentes
- Les données sont visibles
- Environ 60,000 lignes pour FactInternetSales

---

## PARTIE 4 : Créer et vérifier les relations

### Étape 4.1 : Ouvrir la vue Diagramme

**Option 1** :

- Menu **Modèle** → **Vue du modèle** → **Vue de diagramme**

**Option 2** :

- Bouton dans la barre d'outils : icône avec des tables reliées

**Résultat** : Vous voyez toutes les tables sous forme de diagramme

```
┌─────────────┐       ┌──────────────────┐       ┌─────────────┐
│  DimDate    │       │ FactInternetSales│       │ DimProduct  │
├─────────────┤       ├──────────────────┤       ├─────────────┤
│ DateKey (PK)│━━━━━━▶│ OrderDateKey (FK)│       │ProductKey   │
│FullDateAlt..│       │ CustomerKey (FK) │◀━━━━━━│ProductSub.. │
│ CalendarYear│       │ ProductKey (FK)  │       │ EnglishPr.. │
│ MonthName   │       │ SalesAmount      │       └─────────────┘
└─────────────┘       │ OrderQuantity    │
                      └──────────────────┘
```

### Étape 4.2 : Vérifier les relations automatiques

**Visual Studio crée automatiquement les relations** basées sur les noms de colonnes.

**Pour vérifier** :

1. Dans **l'Explorateur de modèles**, développer **"Relations"**

Vous devriez voir :

```
Relations
├─ FactInternetSales → DimDate (OrderDateKey → DateKey)
├─ FactInternetSales → DimProduct (ProductKey → ProductKey)
├─ FactInternetSales → DimCustomer (CustomerKey → CustomerKey)
├─ FactInternetSales → DimSalesTerritory (SalesTerritoryKey → SalesTerritoryKey)
├─ DimProduct → DimProductSubcategory (ProductSubcategoryKey → ProductSubcategoryKey)
├─ DimProductSubcategory → DimProductCategory (ProductCategoryKey → ProductCategoryKey)
├─ DimCustomer → DimGeography (GeographyKey → GeographyKey)
└─ FactResellerSales → DimDate (OrderDateKey → DateKey)
```

### Étape 4.3 : Créer une relation manuelle (si absente)

**Si une relation manque**, par exemple FactInternetSales → DimDate :

1. Dans la **vue Diagramme**
2. **Cliquer** sur la colonne **OrderDateKey** dans **FactInternetSales**
3. **Maintenir le clic** et **glisser** vers la colonne **DateKey** dans **DimDate**
4. **Relâcher** : la relation se crée automatiquement

**OU via l'interface** :

1. Menu **Modèle** → **Gérer les relations**
2. Cliquer **"Nouvelle"**
3. Configurer :
   - **Table** : FactInternetSales
   - **Colonne** : OrderDateKey
   - **Table associée** : DimDate
   - **Colonne associée** : DateKey
   - **Cardinalité** : Plusieurs à un (*:1)
   - **Direction du filtre croisé** : Simple
4. Cliquer **OK**

### Étape 4.4 : Valider toutes les relations

**Liste complète des relations nécessaires** :

| Table source          | Colonne               | Table cible           | Colonne               | Type           |
| --------------------- | --------------------- | --------------------- | --------------------- | -------------- |
| FactInternetSales     | OrderDateKey          | DimDate               | DateKey               | *:1            |
| FactInternetSales     | DueDateKey            | DimDate               | DateKey               | *:1 (Inactive) |
| FactInternetSales     | ShipDateKey           | DimDate               | DateKey               | *:1 (Inactive) |
| FactInternetSales     | ProductKey            | DimProduct            | ProductKey            | *:1            |
| FactInternetSales     | CustomerKey           | DimCustomer           | CustomerKey           | *:1            |
| FactInternetSales     | SalesTerritoryKey     | DimSalesTerritory     | SalesTerritoryKey     | *:1            |
| DimProduct            | ProductSubcategoryKey | DimProductSubcategory | ProductSubcategoryKey | *:1            |
| DimProductSubcategory | ProductCategoryKey    | DimProductCategory    | ProductCategoryKey    | *:1            |
| DimCustomer           | GeographyKey          | DimGeography          | GeographyKey          | *:1            |
| FactResellerSales     | OrderDateKey          | DimDate               | DateKey               | *:1            |
| FactResellerSales     | ProductKey            | DimProduct            | ProductKey            | *:1            |
| FactResellerSales     | SalesTerritoryKey     | DimSalesTerritory     | SalesTerritoryKey     | *:1            |

**Note** : Les relations sur DueDate et ShipDate sont **inactives** car il ne peut y avoir qu'une seule relation active entre deux tables.

---

## PARTIE 5 : Traiter (Process) le modèle

### Étape 5.1 : Traiter les tables

**IMPORTANT** : Après l'importation, vous devez "traiter" le modèle pour qu'il soit utilisable.

1. Dans **l'Explorateur de solutions** (Solution Explorer)
2. **Clic droit** sur le nom du projet : **AdventureWorksTabular**
3. Sélectionner **"Traiter le modèle"** (Process Model) ou **"Traiter la base de données"**

**OU** :

1. Menu **Modèle** → **Traiter** → **Traiter tout**

### Étape 5.2 : Fenêtre de traitement

Une fenêtre s'ouvre :

```
┌─────────────────────────────────────────────┐
│ Traiter le modèle tabulaire                 │
├─────────────────────────────────────────────┤
│ Mode : Traiter tout                         │
│                                             │
│ ☑ DimCustomer                               │
│ ☑ DimDate                                   │
│ ☑ DimGeography                              │
│ ☑ DimProduct                                │
│ ☑ DimProductCategory                        │
│ ☑ DimProductSubcategory                     │
│ ☑ DimSalesTerritory                         │
│ ☑ FactInternetSales                         │
│ ☑ FactResellerSales                         │
│                                             │
│        [Annuler]  [OK]                      │
└─────────────────────────────────────────────┘
```

2. Vérifier que **toutes les tables sont cochées**
3. Cliquer **OK**

### Étape 5.3 : Progression du traitement

```
Traitement en cours...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 75%

Traitement de DimDate... Réussi
Traitement de DimProduct... Réussi
Traitement de FactInternetSales... Réussi (60,398 lignes)
Traitement de FactResellerSales... Réussi (60,855 lignes)
...

✓ Traitement terminé avec succès
```

**Résultat attendu** : Message "Traitement terminé avec succès"

---

## PARTIE 6 : Déployer sur le serveur SSAS

### Étape 6.1 : Configurer le déploiement

1. Dans **l'Explorateur de solutions**, **clic droit** sur le projet
2. Sélectionner **"Propriétés"**
3. Dans la fenêtre des propriétés :

```
Propriétés : AdventureWorksTabular
├─ Serveur de déploiement : localhost
├─ Base de données de déploiement : AdventureWorksTabular
├─ Mode de traitement : Par défaut
└─ Option de traitement : Traiter tout
```

4. **Serveur de déploiement** : `localhost` (votre serveur SSAS)
5. **Base de données** : `AdventureWorksTabular`
6. Cliquer **OK**

### Étape 6.2 : Déployer

1. **Clic droit** sur le projet dans l'Explorateur de solutions
2. Sélectionner **"Déployer"** (Deploy)

**OU** :

1. Menu **Générer** → **Déployer AdventureWorksTabular**

### Étape 6.3 : Progression du déploiement

```
Déploiement en cours...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Connexion au serveur...
Création de la base de données...
Déploiement des tables...
Déploiement des relations...
Traitement des données...

✓ Déploiement réussi
```

---

## PARTIE 7 : Vérifier le déploiement dans SSMS

### Étape 7.1 : Se connecter à SSAS

1. Ouvrir **SQL Server Management Studio (SSMS)**
2. Fenêtre de connexion :
   - **Type de serveur** : **Analysis Services**
   - **Nom du serveur** : `localhost`
   - **Authentification** : Authentification Windows
3. Cliquer **Connexion**

### Étape 7.2 : Vérifier la base de données

Dans **l'Explorateur d'objets** :

```
localhost (Analysis Services)
├─ 📁 Bases de données
│  └─ 📊 AdventureWorksTabular          ← Votre modèle déployé !
│     ├─ 📁 Dimensions
│     ├─ 📁 Groupes de mesures
│     ├─ 📁 Rôles
│     └─ 📁 Sources de données
```

**✅ Si vous voyez AdventureWorksTabular** : Déploiement réussi !

### Étape 7.3 : Tester avec une requête DAX

1. **Clic droit** sur **AdventureWorksTabular**
2. Sélectionner **"Nouvelle requête"** → **"DAX"**
3. Entrer cette requête de test :

```dax
EVALUATE
SUMMARIZECOLUMNS(
    DimDate[CalendarYear],
    "Total Sales", SUM(FactInternetSales[SalesAmount])
)
```

4. Cliquer **Exécuter** (F5)

**Résultat attendu** :

```
CalendarYear    Total Sales
2011            9,791,060.30
2012            9,770,899.74
2013            9,791,060.30
2014            (vide ou partiel)
```

**✅ Si vous voyez des résultats** : Tout fonctionne !

---

## PARTIE 8 : Créer votre première mesure DAX

### Étape 8.1 : Retour dans Visual Studio

1. Dans Visual Studio, ouvrir la **vue Grille de données**
2. Cliquer sur la table **FactInternetSales**

### Étape 8.2 : Créer une mesure

**Méthode 1 : Via la grille de mesures**

En bas de la vue tableur, vous voyez une grille vide :

```
┌────────────────────────────────────────┐
│ Grille de mesures                      │
├────────────────────────────────────────┤
│ (Cliquez pour ajouter une mesure)      │ ← Cliquer ici
└────────────────────────────────────────┘
```

1. Cliquer dans une cellule vide
2. Taper :

```dax
Total Internet Sales:=SUM(FactInternetSales[SalesAmount])
```

3. Appuyer sur **Entrée**

**Méthode 2 : Via le menu**

1. Dans **l'Explorateur de modèles**, développer **"Tables"**
2. **Clic droit** sur **FactInternetSales**
3. Sélectionner **"Créer une mesure"**
4. Dans la fenêtre :
   - **Nom** : `Total Internet Sales`
   - **Formule** : `=SUM(FactInternetSales[SalesAmount])`
5. Cliquer **OK**

### Étape 8.3 : Vérifier la mesure

1. Dans **l'Explorateur de modèles**, développer **"Mesures"**
2. Vous devriez voir : **📊 Total Internet Sales**

**✅ Votre première mesure DAX est créée !**

---

## RÉCAPITULATIF : Checklist complète

### ✅ Phase 1 : Préparation

- [ ] SQL Server installé
- [ ] SSAS en mode Tabular installé et démarré
- [ ] AdventureWorksDW restaurée dans SQL Server
- [ ] Visual Studio avec SSDT installé

### ✅ Phase 2 : Création du projet

- [ ] Projet Analysis Services Tabular créé
- [ ] Workspace Server configuré

### ✅ Phase 3 : Importation des données

- [ ] Source de données SQL Server créée
- [ ] 9 tables importées (DimDate, DimProduct, etc.)
- [ ] Données visibles dans la vue Grille

### ✅ Phase 4 : Relations

- [ ] Vue Diagramme ouverte
- [ ] Relations automatiques vérifiées
- [ ] Relations manquantes créées si nécessaire

### ✅ Phase 5 : Traitement

- [ ] Modèle traité avec succès
- [ ] Toutes les tables traitées

### ✅ Phase 6 : Déploiement

- [ ] Propriétés de déploiement configurées
- [ ] Modèle déployé sur le serveur SSAS
- [ ] Déploiement réussi

### ✅ Phase 7 : Vérification

- [ ] Connexion SSMS à Analysis Services réussie
- [ ] Base AdventureWorksTabular visible
- [ ] Requête DAX de test fonctionnelle

### ✅ Phase 8 : Première mesure

- [ ] Mesure "Total Internet Sales" créée
- [ ] Mesure visible dans l'Explorateur de modèles

---

## TROUBLESHOOTING : Problèmes courants

### Problème 1 : "Impossible de se connecter à localhost"

**Solution** :

```powershell
# Vérifier que le service SSAS est démarré
Get-Service *analysis*

# Si arrêté, démarrer :
Start-Service MSSQLSERVER
```

### Problème 2 : "Les tables ne s'importent pas"

**Causes possibles** :

- AdventureWorksDW pas restaurée dans SQL Server
- Mauvais nom de serveur
- Permissions insuffisantes

**Solution** :

1. Vérifier dans SSMS que AdventureWorksDW existe
2. Vérifier les permissions (dbo ou db_datareader minimum)

### Problème 3 : "Relations non créées automatiquement"

**Solution** :

1. Créer manuellement (glisser-déposer dans la vue Diagramme)
2. Ou Menu Modèle → Gérer les relations

### Problème 4 : "Erreur lors du traitement"

**Solution** :

1. Vérifier la connexion à la base source
2. Rafraîchir : Clic droit table → Traiter la table

### Problème 5 : "Le déploiement échoue"

**Causes** :

- Service SSAS arrêté
- Permissions insuffisantes
- Nom de serveur incorrect

**Solution** :

```powershell
# Vérifier le service
Get-Service MSOLAP*

# Vérifier les permissions (administrateur SSAS requis)
```
